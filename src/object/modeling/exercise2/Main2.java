/**
 * La clase Main2 es una de las clases con método main del paquete object.modeling.exercise2,
 * por lo tanto, la podemos elegir como clase principal y punto de entrada para la ejecución del programa.
 */
package object.modeling.exercise2;
public class Main2 {
    /**
     * El método main es el punto de entrada del programa. Recibe un arreglo de Strings
     * como argumento que no se utiliza en este caso.
     *
     * @param args Arreglo de Strings que se recibe como argumento pero no se utiliza.
     */
    public static void main(String[] args) {
        // Imprime el mensaje en la consola indicando que La clase Main2 es el punto de entrada del programa
        System.out.println("La clase Main2 es el punto de entrada del programa...");
    }

}
